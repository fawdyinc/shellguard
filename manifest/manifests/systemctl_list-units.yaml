name: systemctl_list-units
description: List units currently in memory
category: services
flags:
  - flag: "--type"
    takes_value: true
  - flag: "--failed"
  - flag: "--no-pager"
stdin: false
stdout: true
