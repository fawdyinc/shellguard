name: sort
description: Sort lines of text files
category: text_processing
shell: bash
flags:
  - flag: "-n"
  - flag: "-r"
  - flag: "-k"
    takes_value: true
allows_path_args: true
stdin: true
stdout: true
